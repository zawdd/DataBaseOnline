#include <stdio.h>

int main()
{
    int n;
    float a,b;
    float area;
    scanf("%d",&n);
    while(n-->0){
        scanf("%f %f", &a, &b);
        area = a*b;
        printf("%f/n", area);
    }
}
